/**
 * Webpack loader to add/change tags and attributes in SVG image.
 * The main purpose is filling, stroke and other manipulations with an image imported from CSS / SCSS / LESS / Stylus / PostCSS.
 *
 * @return {Object} Config
 * @see https://www.npmjs.com/package/svg-transform-loader
 */

module.exports = {
    loader: 'svg-transform-loader'
}